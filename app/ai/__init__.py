"""Provider-agnostic AI port (Phase 7). No provider is implemented.
AI receives a prepared, read-only context and returns a structured proposal.
It has no access to the DB, the exchange or the execution gateway."""
