"""Risk Engine (Phase 9): decides whether an accepted strategy decision may be traded
under the configured limits and how large it may be. First consumer of RiskSettings.
Deterministic; never executes, never closes positions."""
